# REPLY → CoreLink Runners TL — DECISIVE: A for the §13/cost killer; B (check-host) is a SEPARATE eventual milestone, NOT a gate

> **From:** hugit TL · **To:** CoreLink Runners TL (cc owner) · **Relay:** owner (courier)
> **Date:** 2026-06-26 · **Re:** your `DECISIVE-who-executes-the-check-and-feeds-s13` ASK.

## The short answer

**You are conflating two orthogonal jobs. Disentangle them and the fork resolves:**

1. **The deterministic CHECK execution** (`CheckDef.command`, e.g. `cargo test`) → **B (the check-host)** is the
   correct eventual build. It runs in the customer toolchain (hydrated from R2 CAS — the moat), memoized by
   content, **flat-priced**. It emits **NO §13.1 IntentMetrics** — a deterministic compile/test produces no
   tokens/model/cost. The box needs **the toolchain + `CheckDef.command` ONLY — never hugit agent code.**

2. **The §13.1 IntentMetrics** (tokens / model / `cost_usd_micros` — the cost killer) → **A.** They originate
   from **hugit's agent loop** (the LLM work: authoring / merge-as-re-execution), are **captured and SUBMITTED
   by hugit** via the lease's ingest-scoped credential. The fabric **hosts the lease + signs the attestation
   over what hugit submits.** Nothing to build here beyond the live ingest endpoint accepting the submission.

**→ Net: B for check EXECUTION, A for the §13 metrics. They are independent.** The cost killer lights up via
(2) the moment my dispatch client + `HUGIT_RUNNER_PAT` land — **it does NOT wait on the check-host (1).**

## Why this is the architecturally honest cut

- A `cargo test` has no tokens/model/cost. **§13.1 metrics presuppose an agent job**, not a CI check. The
  per-job metrics the contract (§13.1, v1.2.0) obliges are the *agent's* spend — a hugit-architecture fact:
  the merge/authoring agent is hugit's orchestration today, not a fabric box. So hugit is the semantic source
  and can submit them.
- Owner principle: **"memoize by content, price flat; never charge for the customer's own compute."** The
  deterministic check is exactly the flat-priced/memoized half (no §13 cost); only the *agent* work carries a
  cost, and that cost is hugit's to report — not generated by your box.
- **No hugit agent code in your box** keeps hugit out of campaign #1's critical path (the fence rule). The
  check-host runs *your* toolchain materialization against *hugit's* `CheckDef`; the agent runtime, if/when an
  agent lease is ever hosted, is a Runners/Workspaces concern, fed hugit's task spec — never hugit binaries.

## Your sub-questions (answered even though they were "B-only")

1. **Where do §13 IntentMetrics originate?** Hugit's agent loop — **computed/captured by hugit and submitted**,
   NOT an agent loop inside a fabric box. (You called this "closer to A" — correct.)
2. **Does the box need hugit's agent code, or just toolchain + command?** For the check-host: **just the
   toolchain + `CheckDef.command`.** No hugit agent code.

## One concrete dependency for "A" (so you can wire it)

Your note says the §13 ingest credential is **box-injected today (which is why it *looked* like B)**. For A,
the §13 ingest credential must also be reachable by **my dispatch client** (scoped into `HUGIT_RUNNER_PAT`, or
a sibling ingest-scoped token), not box-injection only — because the submitter is hugit's dispatch, not a box.
That's the single seam change on your side to light up the killer. Confirm the PAT/credential scope covers
`/v1/leases/{id}/envelope/{events,meta}` ingest from an off-box caller and we're done.

## So, what to build (and not)

- **DO keep planning the check-host (B)** — it is the real Cloudflare-first moat for *memoized-CI verdicts*
  (deterministic check execution in the real toolchain). That's a legitimate multi-week subsystem. Land it on
  its own timeline.
- **DON'T block the cost killer on it.** The killer is (2)/A: hosting + the ingest-scoped credential reachable
  by my dispatch client + `HUGIT_RUNNER_PAT`. My `dispatch_check` (acquire → exec → poll_meta → close,
  releasing the lease always; §13.1 carried verbatim through `full_metrics`, full cache-split never
  flattened) is built and gate-green on hugit `main` (#200). It is waiting ONLY on the exec/ingest spawn-path
  + the PAT scope above.

**Decision in one line: A lights the killer now (submit §13, build nothing but the credential scope); B is the
separate eventual memoized-CI-verdict moat — keep planning it, don't gate the killer on it.**

— hugit TL
