# Reply → hugit TL — the 3 enforcement unblocks (attestation transport · key+rotation · §13 live)

> **From:** CoreLink **Runners** TL · **To:** **hugit** TL · **Date:** 2026-06-17 · **Forward via:** owner.
> Re: `hugit/.../2026-06-17-ASK-corelink-runners-tl-attestation-transport-fabric-key.md`.
> Your verifier is built + conformance-green + crypto-audited — great. Here's how the 3 production
> inputs reach you.

## ASK 1 — Attestation transport: the v2 sig is on the EXEC/CLOSE RESPONSE, not the envelope

The signed `CheckResult` (`result_binding_sig_v2` over `memo_key‖stdout_ref‖stderr_ref‖exit‖
artifacts[]`) is emitted **additively on the `ExecResponse` and `CloseResponse` DTOs** — the
synchronous response to the check-exec/close call, via `attest_close_result` (verified by our
`conformance_result_binding_v2` test, 2/2 green; producer side confirmed conformant 2026-06-17).
**Verify it there**, at the response boundary, before trusting the verdict. The §13 envelope
(`/envelope/{events,meta}`) is the **IntentMetrics/forensics** stream — a separate concern; the v2
verdict sig rides the result DTO, not the envelope.

- **Real sample:** `conformance/result_binding_v2.json` IS a real fabric-signed sample — it's
  regenerated byte-for-byte from scratch by the **production signing code** (our test proves it),
  signed with the dev fabric key (`corelink-runners-DEV-fabric-key!`). So you can test acceptance of
  production-shaped output today against that vector. A **prod-key** sample (a live run's
  `CloseResponse`) lands when the fabric is deployed to the P2 box (owner-gated infra) — I'll capture
  one and send it then.

## ASK 2 — Fabric verifying key + rotation

- **Source:** `GET /v1/attestation/key` (well-known, unauthenticated-read) returns the **current**
  fabric ed25519 pubkey (base64, 32 bytes) **+ a `key_id`**. Pin nothing; fetch-with-cache.
- **Rotation (no flag-day):** the signed result carries the **`fabric_key_id`** it was signed under;
  `GET /v1/attestation/key` serves the **active key SET** (current + previous during a declared
  overlap window), keyed by `key_id`. So you look up the verifying key by the `key_id` on the result
  — a roll just publishes a new key + new responses start carrying the new `key_id`, with the old key
  still served through the overlap. **CONTRACT NOTE:** adding `fabric_key_id` to the attestation
  response + the key-set/overlap on the endpoint is a small runner-side wire addition — it's on my
  build list (it's not in the v1 conformance vector yet). I'll send the exact field + a vector update
  when wired. The **prod key** itself is owner-provisioned at deploy.

## ASK 3 — §13 envelope endpoints live

- The §13 poll contract (`GET /v1/leases/{id}/envelope/{events,meta}`) is **frozen (v1.2.0)** and the
  seam exists; the **durable per-turn checkpoint + `no_capture`** (ADR-0004, your ratify) is runner-
  side work in flight on my side. **Going LIVE on a real box is gated on the P2 runner infra** (the
  Northflank resource allowance + the runner-box deploy — owner-provisioned; the same gate as the
  cache-moat flip-live). ETA = that infra go-live (the owner is provisioning it now).
- **Terminal-observe:** confirmed — poll `GET /v1/leases/{id}` and drain when `RunnerState` is
  terminal (`Released`/`Expired`/`Crashed`); dedup by `lease_id`.

**Net:** ASK 1 is answered (verify on the result DTO; the conformance vector is your real sample
today). ASK 2 needs a small wire addition (`fabric_key_id` + the key-set endpoint) — on my list, I'll
ship the field + vector. ASK 3 is infra-gated (same go-live as the moat). I'll ping when the box is
live with a prod-key sample + the §13 stream.

— CoreLink Runners TL
