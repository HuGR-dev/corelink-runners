# Monitor runtime support — root frozen decisions

Baseline 3627029dbf92e4920077be77ec9fa28f89e9d7cb (approved foundations, audit/outbox/witness). These are bounded auxiliary T6-W15 source paths; no release, AWS resource mutation, message sending, or go-live credit. Node22 ESM AWS SDK3 exact dependencies already installed. Separate worktrees/owners; real-source composition only, no type suppressions or fake declaration files.

## Durable trusted-time floor

src/durable_time_floor.ts exports DurableTimeFloor({store:MonitorStateStore,key:string,minimumTimeMs:number}). load():Promise<number>; commit(expected:number,next:number):Promise<boolean>. Use actual StateStore get/transact; exact stored value {version:'1',timeMs:number}. minimumTimeMs must be safe positive integer from configured qualified TSA proof, not0/localDate.now. Missing record initializes to minimum via expectedVersion:null CAS, then reloads on conflict, bounded8 attempts. Reject malformed/unsafe/less-than-configured-minimum state; never silently reset corrupt or regressed existing state. commit validates safe next>=expected>=minimum, loads exact current stored value, returnsfalse on known value mismatch/CASconflict, throws typed backend/corruption errors otherwise. Store transaction expectedVersion matches read version; no blind overwrites and no in-memory production fallback. Tests100concurrentinitialize, competingCAS, existingfloorpreservedacrossinstances, lowerfloor/configminimumdriftrefused, timeout/ambiguouserror nofalsecommit. Runtime passes floor.load/floor.commit as functions into Rfc3161Clock.

## AWS source secret and witness adapters

src/aws_adapters.ts exports AwsSourceSecrets({client:SecretsManagerClient,timeoutMs<=5000}) implementing load(registration:SourceRegistration):Promise<Uint8Array>. GetSecretValue uses exact SecretId=secretArn and VersionId=secretVersionId, never AWSCURRENT. Validate returned ARN and VersionId exactly before exposing decoded key. SecretString JSON exactfields {version:'1',source,service,application,key_id,credential_epoch,hmac_key_base64url}; scope/key/epoch must match registration, version1; base64url canonical nonempty32..1024bytes. Strict parsed object/extras/string bound4096bytes; no SecretBinary fallback. No secret/rawSDKerror in outputs/logs. AbortSignal deadline<=5000, fixed typed sanitized errors, optional memoization only exact ARN/version/scope tuple. Production config contains no secret itself.

LambdaWitnessClient({client:LambdaClient,functionArn:string,logId:string,journalIdentity:PublicSigningIdentity,witnessIdentity:PublicSigningIdentity,timeoutMs<=5000,maxResponseBytes<=262144}) implements CurrentCheckpointWitness.accept/readHead. Qualified function ARN MUST end positive decimal immutable version, account must equal witnessIdentity.keyArn account, region match configuredclientregion if available; witness role iswitness and journal role isjournal with different accounts/keys. Reject invalid checkpoint locally (exactfields/signature/identity/logId) before invoking. InvokeFunctionName exactARN, InvocationType RequestResponse, Payload UTF8 JSON {action:'accept',checkpoint} or {action:'head',nonce}. Response requires StatusCode200, no FunctionError, ExecutedVersion exactly requestedversion, Payload nonempty and <=configuredcap BEFORE JSONparse; AWS SDK service transport may buffer its provider-bounded reply, so do not claim a256KiB streaming allocationcap. Root/runtime requestresponses are tiny; no arbitrary serviceendpointoverride. Abort and monotonic duration bound<=5000 coversI/O andresponseverification. accept verifies exact returned WitnessReceipt signature/identity/log/sequence/previousWitnessRoot andcheckpointRoot binding (callerlog checkpoint known; witnesspreviousroot isverifiedsigned but monitorAuditLoglinks expectedpreviousroot separately). readHead verifies SignedWitnessHead signature/identity/log/exactrequestednonce/strictsafetimestamp and sequence/rootgenesisrules. No valid|true verificationcallback in production; real Node RSA-PSS verifyOrderedFields. No retrythatreturnsoldcachedhead; caller mayretryfreshnonceexplicitly. Invalid/ambiguousreply typederror; no fallback tolocal witness/signing.

Tests src ownership only aws_adapters.ts plus test/aws-adapters.test.ts. Mock SDK transport while exercising real signedcheckpoint/head/receipt RSA-PSS fixtures and real secretencoding; verify exact AWScommandargs, qualifiedversionresponse, wrongidentity/domain/nonce/sig/oversize/body/error/timeout, zeroInvoke oninvalidrequest, no secretleakerrorstrings. Unit mocks don't establish IAM isolation or deployed capability.

## Witness Lambda runtime

src/witness_runtime.ts exports validateWitnessConfig(input:unknown):WitnessConfig, createWitnessHandler(config:WitnessConfig, dependencies?:WitnessRuntimeDependencies), and handler(event,context). Environment WITNESS_CONFIG_JSON is the sole production config payload, version-bound by deployment. Config exactfields {version:'1',region,monitorAccountId,verifierAccountId,stateTable,stateNamespace,journalBucket,journalPrefix,journalRetentionMs,allowedLogIds:string[],journalIdentity,witnessIdentity,trustedTime}. trustedTime uses existing Rfc3161Clock config fields plusminimum/maxadvance; productionendpoint exactDigiCert, twoCRLURLs/pinnedroot/intermediate, no injectedverifier callback/defaulttime. AccountIDs distinct, witnesskeyARN inverifier and journalkeyARN inmonitor, rolesdistinct, retention>=8days, strings/ARNs/idsbounded and validatedrecursively. No standaloneclaimthat configecho provesliveaccountidentity.

Production factory constructs DynamoDbStateStore(DynamoDBClient), S3ImmutableJournal(S3Client), AwsKmsSigner(KMSClient,witnessIdentity), DurableTimeFloor withfixedclockfloorkey, and Rfc3161Clock usingthatfloor, then one DurableCheckpointWitness per explicit allowedlogId under disjointDDBnamespace/S3prefix. Event exact {action:'accept',checkpoint} or {action:'head',nonce,logId}; accept deriveslogId fromsignedcheckpoint; headmustcarry explicitlogId. IMPORTANTadapter head DTO extends above to include logId (bothsides exact). Refuse unknownlog/extra/malformedbody, unqualified/$LATEST context.invokedFunctionArn, wrongaccount/region, beforewrite. Onsuccess return raw WitnessReceipt/SignedWitnessHead. Onerror throw fixed sanitized error forLambda FunctionError, never return mocksuccess/status200error. No journal/signature/requestbody logging. Dependency injection for tests must exercise real witnessclass with real state/journal/signers/proofclock fixtures; defaultproductionfactory remains concrete SDK wiring. Neitheraccept norhead invokes SNS orCloudflare.

Ownership witness_runtime.ts and test/witness-runtime.test.ts only; do not edit package/index/infra or foundationowners. Tests strictconfig/account/role/contextversion/logrouting, realaccept/headclassdispatch, malformedrequestzeroeffects, dependencyfailuretypedLambdaerror; liveIAMpermission/provisioning is a separate finalgate.
