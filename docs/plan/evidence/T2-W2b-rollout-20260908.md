# T2-W2b rollout result — 2026-09-08

The requested rollout was attempted from clean tip `15d85a5dd703f4a64101b7de64a2e5b8a3ba7815` after a no-force idle gate (`busy=0`, `checked=0`, `unverifiable=0`). Wrangler uploaded the Worker and created version `9a456fa5-e30b-44cc-a638-f78da8fc02cc`, but failed to complete the container application rollout with:

> Could not deploy container application as durable object was not found in list of bindings

The remote application list confirmed that `RunnerDevEnvDO` was not created. The Worker was therefore rolled back to version `43df9d2b-7a83-437b-ba11-aa40ddef1b42` at 100% through deployment `269cf612-f9df-4ef1-b944-59e4417c4c59`. Post-rollback unauthenticated routes returned the expected `401`, RunnerContainer and CheckHost refs were unchanged, Fabricd was untouched, and the no-force idle gate passed again (`busy=0`, `checked=0`, `unverifiable=0`).

This is a rollback-recovered failure, not a successful T2-W2b rollout. The remaining blocker is the Cloudflare Durable Object/container binding required to create `RunnerDevEnvDO`.
