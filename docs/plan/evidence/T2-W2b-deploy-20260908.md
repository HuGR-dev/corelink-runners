# T2-W2b canonical deploy evidence — 2026-09-08

## A2.5 — PASS

Wrangler OAuth inspected `corelink-fabricd-fabricdcontainer` (UUID `a0337af9-26db-42c0-a816-0a63ba0e58a3`, version 26). Its configured image and running instance report the same immutable digest:

`registry.cloudflare.com/6a1fc1c626fc2628823e60b9db01f5cd/corelink-fabricd-fabricdcontainer@sha256:2e7bcea926f4ce2b38edb1a381f3821fcf4c898377e4f988b763fd3232c0e565`

The provider reported instance `12e222c7ce4f7e3854933755482a6d74d8ff9b14bec171966f0a5947984db3f4` in state `running`, version 26, location `gig10`. Both `/health` and `/v1/health` returned HTTP 200 with `ok`. The repository records build SHA `3f7afa9` for this image and proves #515 commit `3131858` is its ancestor.

## A2.7 — RED, specific

The documented CI path is `.github/workflows/deploy-spawn-worker.yml`: `npm ci`, `npm run typecheck`, `npm run test`, fleet gate, then `npx wrangler deploy`. At `9273080e0485a202bdca5f1c2899e5169fb48e45`, the authorized local test/typecheck gate is green: Vitest completed 90 files and 1119 tests with no failures in 26.67 seconds, and typecheck passed.

The earlier RED result remains part of the evidence: 85 passed files, 5 failed files, 1109 passed tests and 10 failed tests out of 1119, including `reconciler-wave-acceptance`, `retry-epoch-integration`, and `teardown-permutation` plus two files retained in the original Vitest summary. The current source tip has not completed the documented CI deploy path; the recorded Worker deployment predates G1's production admission-budget change. Therefore A2.7 remains RED and receives no green credit until the documented path deploys this tip and captures the new Worker's `/health` response.

The subsequent local deploy had already completed on Worker version `cb704a9d-f6e0-45d7-9e68-89a263a77993` at 100%, with DevEnv application `a037c709-9f21-493c-8dcb-2414a588a1cd` ready on the immutable `d105e11f...` image. Pre/post fleet gates were `0/0/0`, `force=false`; intake and redrive remained paused at `1/1`. These facts prove deployment behavior and safety gates, but do not override the failed CI test criterion.
