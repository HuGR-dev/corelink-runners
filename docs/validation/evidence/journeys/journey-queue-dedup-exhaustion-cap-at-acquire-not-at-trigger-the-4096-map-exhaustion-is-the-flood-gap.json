{
  "journey": "Queue-dedup exhaustion — cap-at-acquire not-at-trigger; the 4096-map exhaustion is the flood gap",
  "sid": [
    "S7.15"
  ],
  "persona": "P7 attacker",
  "atoms": [
    "F-4.9",
    "F-5.2"
  ],
  "pass": true,
  "failedAt": null,
  "steps": 2,
  "narrative": [
    {
      "step": "POSITIVE CONTROL — the concurrency cap (enforced at ACQUIRE) is a finite bound",
      "ok": true,
      "assertion": "cap = 10 enforced at acquire (a trigger operates only on an already-admitted lease)",
      "artifact": {
        "cap": 10
      }
    },
    {
      "step": "ATTACK — the trigger endpoint is not an anon surface: no PAT → 401 (tenant-scoped)",
      "ok": true,
      "assertion": "anon trigger → 401 (authed, tenant-scoped — the dedup key folds tenant)",
      "artifact": {
        "status": 401,
        "gap": "The TRIGGER_DEDUP_CAP=4096 insertion-capped map (at-cap: serve-but-stop-memoizing → a later duplicate re-executes as a real, separately-metered, ceiling-bounded job) can only be exercised by a large flood of distinct (item_id, tree_hash) triggers on real leases.",
        "reachableFrom": "a hugit-driven trigger flood on admitted leases — not a single API probe; and every re-exec is itself capped+metered, so waste is ceiling-bounded, never unbounded."
      }
    }
  ],
  "ts": "2026-07-19T21:43:09.776Z"
}
