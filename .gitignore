# Rust (once code exists)
/target
**/*.rs.bk
Cargo.lock.orig

# TechLead profile — never committed (owner mandate)
/.techlead/

# OS / editor
.DS_Store
*.swp

# Agent worktrees / scratch
/.claude/worktrees/
