{
  "name": "corelink-e2e-signup",
  "private": true,
  "description": "Undercover organic-signup journey (Playwright) — drives the REAL humangr.com signup, then runs the /v1 lifecycle as a brand-new tenant.",
  "type": "module",
  "scripts": {
    "discover": "playwright test 00-discover-runner-console --reporter=list",
    "undercover": "playwright test 10-undercover-signup-to-lease --reporter=list",
    "test": "playwright test --reporter=list"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@clerk/testing": "^1.4.0"
  }
}
