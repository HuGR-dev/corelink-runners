{
  "name": "corelink-canary-cf",
  "version": "0.0.0",
  "private": true,
  "description": "corelink-canary — scheduled Cloudflare Worker that watches the fabricd + spawn-worker golden counters and emails the owner (Resend) on breach.",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.103.0"
  }
}
