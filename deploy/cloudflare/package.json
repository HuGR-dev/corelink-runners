{
  "name": "corelink-spawn-worker",
  "version": "0.0.0",
  "private": true,
  "description": "CoreLink spawn-Worker + Container DO — Cloudflare substrate (ADR-0008). SKELETON, not yet deployed.",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  },
  "//": "Versions are PLACEHOLDERS — pin @cloudflare/containers + wrangler to real releases at the first live build."
}
