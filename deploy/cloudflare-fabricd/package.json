{
  "name": "corelink-fabricd-cf",
  "version": "0.0.0",
  "private": true,
  "description": "corelink-fabricd control plane on Cloudflare Containers (gap-#1 option b) — proxy Worker + singleton container DO.",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.103.0"
  }
}
